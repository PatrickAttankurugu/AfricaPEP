services:
  api:
    volumes:
      - .:/app
    command: uvicorn africapep.api.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - ENVIRONMENT=development

  scraper:
    volumes:
      - .:/app
